language: node_js
node_js:
  - "8"
script:
  - npm test
  - npm run build
  - echo $ADMIN_TOKEN > ADMIN_TOKEN
deploy:
  skip_cleanup: true
  - provider: heroku
    app: cloudboard-app
    api_key:
      secure: $HEROKU_API_KEY
  - provider: firebase
    api_key:
      secure: $FIREBASE_TOKEN
